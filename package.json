{
  "name": "deepseek-harness-desktop",
  "version": "1.1.0",
  "description": "DeepSeek Harness Desktop：DSH 桌面包装器、Wallpaper Engine 壁纸选择器与解析助手",
  "main": "main.js",
  "author": "DeepSeek Harness Desktop contributors",
  "license": "MIT",
  "private": false,
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lumeng-Agt/deepseek-harness-desktop.git"
  },
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check config.js && node --check path-discovery.js && node --check lib/path-utils.js && node --check wallpaper-helper.js",
    "test": "node --test",
    "audit": "npm audit --audit-level=high",
    "pack": "electron-packager . \"DeepSeek Harness Desktop\" --platform=win32 --arch=x64 --icon=icon.ico --out=release --overwrite --prune=true",
    "pack:all": "electron-packager . \"DeepSeek Harness Desktop\" --all --icon=icon.ico --out=release --overwrite --prune=true"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron": "^43.4.0"
  }
}
